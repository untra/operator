# Site settings
title: Operator! Documentation
description: Documentation for Operator! - combining kanban workflows with LLM tools
url: "https://operator.untra.io"
baseurl: ""

# GitHub repository
github:
  repo: "https://github.com/untra/operator"
  author: "https://github.com/untra"

# Build settings
markdown: kramdown
highlighter: rouge

# Exclude from build
exclude:
  - Gemfile
  - Gemfile.lock
  - vendor

# Plugins
plugins:
  - jekyll-seo-tag

# Defaults
defaults:
  - scope:
      path: ""
    values:
      layout: "doc"

# Collections
collections_dir: .

# Permalink structure
permalink: pretty
version: 0.1.6